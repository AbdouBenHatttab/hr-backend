spring.application.name=hr-backend
server.port=8081

# Keycloak Configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/hr-realm

# Keycloak Admin Configuration (for user registration)
keycloak.auth-server-url=http://localhost:8080
keycloak.realm=hr-realm
keycloak.admin.username=admin
keycloak.admin.password=admin
keycloak.admin.client-secret=jRr9LouFb63epMwrE2W5lLdbciOvFXob



# Keycloak Client Configuration (for login)

keycloak.client-id=hr-backend
keycloak.client-secret=a1dja7kQI6oS4Cd3A84i5TzExKq6VC8L
# PostgreSQL Database Configuration

# PostgreSQL port is 5433 (mapped from container port 5432)
spring.datasource.url=jdbc:postgresql://localhost:5433/hr_db
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Email Configuration (SMTP)
# Configure your SMTP server settings here
# Gmail SMTP uses port 587 for TLS
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=arabsoft.info@gmail.com
spring.mail.password=dmpescflkdcqbhfx

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Application Settings
app.company.name=HR Management System

# Kafka Configuration
app.kafka.enabled=true
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=hr-backend-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

# Kafka Topics
app.kafka.topic.user-role-assigned=user-role-assigned
app.kafka.topic.password-reset=password-reset

